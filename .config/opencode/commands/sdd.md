---
description: Start an SDD workflow — spec-driven development from explore to archive
agent: sdd-orchestrator
---

Start an SDD workflow for: $ARGUMENTS

If no argument given, ask the user what they want to build or change, then begin the SDD cycle:
explore → propose → spec → design → tasks → apply → verify → archive

Coordinate the appropriate SDD sub-agents in order. Wait for user approval between phases.
